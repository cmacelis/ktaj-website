---
// Server-side code
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>KTAJ - Keep Talking About Jesus</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700;900&display=swap" rel="stylesheet">
  </head>
  <body>
    <nav>
      <div class="logo">KTAJ</div>
      <div class="links">
        <a href="#">Home</a>
        <a href="#">Sermons</a>
        <a href="#">About Us</a>
      </div>
    </nav>

    <main>
      <div class="hero-content">
        <h1>KEEP TALKING<br>ABOUT JESUS</h1>
        <p>Sharing the gospel through media and community.</p>
        <button>Watch Latest Sermon</button>
      </div>
    </main>

    <style>
      /* 1. Reset and Font Setup */
      body { 
        font-family: 'Montserrat', sans-serif; 
        margin: 0; 
        color: white;
        height: 100vh; /* Full screen height */
        display: flex;
        flex-direction: column;
      }

      /* 2. The Cinematic Background Trick */
      /* We use a linear-gradient to darken the image so text pops */
      body {
        background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.8)), url('/bg.jpg');
        background-size: cover;
        background-position: center;
        background-attachment: fixed; /* Parallax effect */
      }
      
      /* Navigation */
      nav { 
        padding: 2rem 3rem; 
        display: flex; 
        justify-content: space-between; 
        align-items: center; 
      }
      .logo { font-weight: 900; font-size: 1.5rem; letter-spacing: 3px; }
      .links a { color: rgba(255,255,255,0.7); text-decoration: none; margin-left: 30px; font-weight: 700; font-size: 0.9rem; text-transform: uppercase;}
      .links a:hover { color: white; }

      /* Hero Content centering */
      main { 
        flex-grow: 1; 
        display: flex; 
        justify-content: center; 
        align-items: center; 
        text-align: center; 
        padding: 1rem;
      }

      /* Typography */
      h1 { 
        font-size: 4.5rem; 
        line-height: 1; 
        margin-bottom: 1rem; 
        font-weight: 900; 
        letter-spacing: -2px;
        text-shadow: 0 4px 10px rgba(0,0,0,0.5); /* Text drop shadow */
      }
      
      p { 
        font-size: 1.5rem; 
        color: rgba(255,255,255,0.9); 
        margin-bottom: 2.5rem; 
        font-weight: 400;
      }
      
      /* Button Styling */
      button { 
        background: white; 
        color: black; 
        border: none; 
        padding: 16px 32px; 
        font-size: 1rem; 
        font-weight: 700;
        border-radius: 50px; 
        cursor: pointer; 
        text-transform: uppercase;
        letter-spacing: 1px;
        transition: transform 0.2s;
      }
      button:hover { 
        transform: scale(1.05); /* Slight zoom on hover */
      }
    </style>
  </body>
</html>